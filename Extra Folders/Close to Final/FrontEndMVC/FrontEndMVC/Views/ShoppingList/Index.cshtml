@model IEnumerable<FrontEndMVC.Models.ShoppingList>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SLID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HHID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SLID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HHID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.SLID }) |
            @Html.ActionLink("Details", "Details", new {  id=item.SLID }) |
            @Html.ActionLink("Delete", "Delete", new {  id=item.SLID })
        </td>
    </tr>
}

</table>
