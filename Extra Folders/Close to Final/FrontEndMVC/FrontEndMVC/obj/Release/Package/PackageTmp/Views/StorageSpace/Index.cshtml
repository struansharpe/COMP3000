@model IEnumerable<FrontEndMVC.Models.StorageSpace>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SSID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StorageName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SSID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StorageName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.SSID }) |
            @Html.ActionLink("Details", "Details", new { id = item.SSID }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.SSID })
            @Html.ActionLink("Filter By Room", "FilterByRoom", new { id = item.RID })
        </td>
    </tr>
}

</table>
